package Graph;

import org.junit.Assert;
import org.junit.Test;

import java.util.ArrayList;

import static org.junit.Assert.*;

public class QueueTest {

    /*@Test
    public void push() {
        Queue q = new Queue();
        for(int i = 0; i < 5; i++)
            q.push(i);

        ArrayList<Integer> list = new ArrayList<>();
        for(int i = 10; i < 20; i++)
            list.add(i);

        q.push(list);

        Assert.assertEquals(15, q.getSize());
    }


    @Test
    public void pull() {
        Queue q = new Queue();

        q.push(1);
        q.push(4);
        q.push(2);

        Assert.assertEquals(q.pull(), 1);
    }*/

}